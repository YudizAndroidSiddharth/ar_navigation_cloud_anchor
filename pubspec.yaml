name: ar_navigation_cloud_anchor
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  http: ^1.5.0
  ar_flutter_plugin: ^0.7.3
  permission_handler: ^10.4.5
  uuid: ^4.5.2
  cloud_firestore: ^6.1.0
  sensors_plus: ^4.0.2
  path_provider: ^2.1.1
  geolocator: ^9.0.2
  flutter_compass: ^0.8.0
  shared_preferences: ^2.3.2
  flutter_blue_plus: ^1.35.0
  beacon_broadcast: ^0.3.0
  wakelock_plus: ^1.1.1
  get: ^4.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/
